name: practicequran
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  # PDF viewer dependency added (Flutter 3.9 compatible)
  syncfusion_flutter_pdfviewer: ^33.1.47

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Assets: PDF + Splash image
  assets:
    - assets/Quran.pdf
